import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  timer: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  button: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  timerProgress: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
